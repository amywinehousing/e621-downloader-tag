# e621-downloader-tag
**A python script to binge download e621 images with corresponding .txt file to each image containing the tags to the art**

***Setup:***
```
pip install requests
```

***Usage:***
```
python main.py --tags "nepentz -rating:s" --count 50 --user yourname --key yourapikey
```
